import React from 'react';
import AccountForm from '../Account_Form/AccountForm';

export default class LoginForm extends React.Component {
  render() {
    return (
      <AccountForm title='Login'/>
    );
  }
}
